# Week 8：结课项目与展示

> **本章导读**
> 时长：3 节课，每节 60 分钟
> 数据：`data/nyc_airbnb.csv`（默认主项目数据，也可选择其他已有数据）
> 交付：可复现分析项目 + `report.md` + 3 分钟口头展示
> 你将学到：用 plan mode 定义项目，用 goal 追踪多轮执行，用 subagent/workflow 并行推进，并完成“问题 → 口径 → 发现 → 局限”的展示

## 1. 跟着老师做

### 1.1 先确定要回答的问题

结课项目不是“让 DSH 做一个项目”，而是先写清楚：

```text
我要回答什么问题？
数据来自哪里？
用什么指标验证答案？
交付文件和验收标准是什么？
```

默认题目方向（围绕 `data/nyc_airbnb.csv`）：

- 不同行政区/房型的定价差异有多大？
- 价格能否被挂牌信息预测？误差主要来自哪里？
- 评论数量、可订天数与价格有什么关系？
- 高价房源的共同画像是什么？

如果学生选择旧数据（空气质量、Titanic、银行营销等），必须自己写清数据来源和口径，不能只贴一份“让 AI 自动做”。

### 1.2 用 plan mode 定义项目

在 DSH 里输入：

```text
/plan 我要做结课项目：<我的题目>。
请先调查仓库里的可用数据，然后给出：
1. 我到底想回答什么问题；
2. 用什么指标衡量答案；
3. 需要哪些字段和清洗步骤；
4. 核心图和表格是什么；
5. 交付文件和验收标准。
先不要创建代码文件。
```

DSH 会先探索、列出方案，并请求确认。学生必须在确认前人工检查三点：

- 问题是否能在数据范围内回答；
- 指标定义是否清楚；
- 是否缺少数据来源和局限。

### 1.3 用 goal 追踪多轮执行

```text
请为我的结课项目创建长期目标：
“完成 <题目> 的可复现分析报告”
验收标准：scripts 可运行、output 有图和表、report.md 有 3 个结论和局限。
先从 plan mode 开始，确认方案后再执行。
```

### 1.4 用 subagent / workflow 并行推进

```text
请同时启动两个后台子代理：
A：审计并清洗我的数据，输出 audit.md；
B：检查我已有的 scripts 是否可运行、是否有明显 bug；
两个子代理完成后分别报告，不要相互等待。
```

## 2. 你自己动手做

1. 把项目拆成独立任务：
   - 数据加载与清洗；
   - EDA；
   - 可视化；
   - 模型或假设验证；
   - 报告撰写。
2. 每次 DSH 完成一个阶段，必须做一次人工验证：
   - 代码是否真的运行；
   - 结论是否真的来自数据；
   - 原始文件是否未被修改。
3. 让 DSH 写 `report.md` 初稿，然后人工修改。
4. 按展示模板准备 3 分钟口头展示。

展示模板：

```text
1. 问题：我要回答什么？为什么值得回答？
2. 数据与口径：数据来自哪里？字段怎么定义？
3. 方法与发现：清洗、EDA、模型或验证，核心 1-2 个结果。
4. 局限：什么不能下结论？
5. 下一步：如果再做一次，会改什么？
```

## 3. 验证清单

- [ ] 项目问题能用当前数据回答
- [ ] 指标定义写进 README
- [ ] 脚本可一键运行，原始数据未修改
- [ ] 至少 1 张核心图、1 张汇总表、1 份 `report.md`
- [ ] 报告区分“已验证事实 / 待验证推断 / 局限”

## 4. 常见错误与坑

| 现象 | 原因 | 处理 |
|---|---|---|
| 题目太大 | 数据和时间都不支持 | 收敛到 1 个可验证问题 |
| 只让 AI 自动完成 | 学生不知道口径 | 每阶段人工验收 |
| 子代理结果口径不一致 | 并行任务没有统一口径 | 停下来统一字段定义 |
| report.md 只写结果 | 没有依据 | 每个结论注明字段、计算、样本量 |
| 展示念代码 | 没按问题组织 | 用“问题 → 口径 → 发现 → 局限” |

## 5. 最终作业

提交：

```text
projects/<姓名>/final/
  README.md
  scripts/
  output/
  report.md
```

`report.md` 结构：

1. 题目与摘要；
2. 数据来源与字段说明；
3. 清洗与口径；
4. 关键发现（带依据）；
5. 局限与待验证假设；
6. 数据伦理声明；
7. 附录：DSH 提示词与迭代记录。

## 评分要点

| 项目 | 权重 | 要求 |
|---|---|---|
| 问题与口径 | 20% | 问题清楚，指标可计算 |
| 可复现 | 25% | 脚本可运行，原数据未改 |
| 分析与图 | 25% | 有依据、有样本量、不误导 |
| 结论与局限 | 20% | 区分事实、推断、局限 |
| 展示 | 10% | 3 分钟讲清闭环 |
