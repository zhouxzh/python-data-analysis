# Week 6：合并、分组与迷你项目

> **本章导读**
> 时长：3 节课，每节 45 分钟
> 数据：`data/06-merge/norway_new_car_sales_by_make.csv`、`data/06-merge/norway_new_car_sales_by_model.csv`、`data/06-merge/MarketArrivals.csv`、`data/06-merge/email_campaign_funnel.csv`
> 你将学到：用 `merge` 连接多张表，用 `concat` 堆叠数据，用 `groupby`、`agg`、`pivot_table`、`crosstab` 做分组聚合，最后完成一份跨领域迷你报告
> 本周产出：`projects/<姓名>/mini_project/`

本周三节课：

1. 第 1 节课：合并两张挪威新车销售表，比较按品牌汇总和按车型汇总是否一致；
2. 第 2 节课：用零售到货数据练习分组、透视与聚合；
3. 第 3 节课：用邮件营销漏斗计算转化率，结合前两节课的方法写一份迷你报告。

本周继续使用 Week 1 的课堂红线：**原始 `data/` 目录只读，代码只写入 `projects/<姓名>/`；先理解计划，再检查命令，小步执行，验证结果。** DSH 说“合并成功”只代表命令没有报错，不代表合并结果正确。合并前必须检查键的唯一性、缺失和重复，合并后必须检查行数、空值和未匹配数据。

```mermaid
flowchart LR
    A[检查两张表的键<br/>唯一性、缺失、重复] --> B[归一化键并合并]
    B --> C[groupby 聚合]
    C --> D[pivot_table / crosstab / 漏斗转化]
    D --> E[写迷你报告并验证]
```

## 1. 第 1 节课：合并两张销售表（45 分钟）

本节课回答一个问题：**按品牌汇总和按车型汇总，挪威新车销量是否一致？**

两张表分别是：

- `data/06-merge/norway_new_car_sales_by_make.csv`：4377 行，字段 `Year`、`Month`、`Make`、`Quantity`、`Pct`
- `data/06-merge/norway_new_car_sales_by_model.csv`：2694 行，字段 `Year`、`Month`、`Make`、`Model`、`Quantity`、`Pct`

先不要写合并代码。让 DSH 只读数据、报告字段和数据质量，审完计划再执行。

### 1.1 发给 DSH 的第一版提示词

```text
请读取 data/06-merge/norway_new_car_sales_by_make.csv
和 data/06-merge/norway_new_car_sales_by_model.csv。
只读，不要修改原文件。
先输出：
1. 每张表的 shape 和字段；
2. 缺失值数量；
3. Year、Month、Make 这组键的重复数量；
4. 两张表 Make 字段的前 5 个原始值（用 repr 显示）。
先不要合并，不要写文件。
```

第一次运行只做审计，不做合并。这叫“小步执行”：每一步都要能看懂命令、能检查输出。

### 1.2 合并前先审计

```python
import pandas as pd

make = pd.read_csv('data/06-merge/norway_new_car_sales_by_make.csv')
model = pd.read_csv('data/06-merge/norway_new_car_sales_by_model.csv')

print('make:', make.shape)
print('model:', model.shape)
print('make missing:', make.isna().sum().sum())
print('model missing:', model.isna().sum().sum())
print('make key duplicates:', make.duplicated(subset=['Year', 'Month', 'Make']).sum())
print('model key duplicates:', model.duplicated(subset=['Year', 'Month', 'Make']).sum())
print('model row duplicates:', model.duplicated().sum())
print([repr(x) for x in model['Make'].unique()[:5]])
```

预期输出：

```text
make: (4377, 5)
model: (2694, 6)
make missing: 10
model missing: 0
make key duplicates: 1
model key duplicates: 1274
model row duplicates: 0
["'Volkswagen '", "'Toyota '", "'Peugeot '", "'Skoda '", "'Ford '"]
```

这四个结果都不能跳过：

1. **make 表有 10 个缺失品牌**，都是 `Make` 为空的零星记录，共 16 辆。没有键就无法按品牌合并，必须先报告，不能直接删掉当没看见。
2. **make 表在 `Year/Month/Make` 上有 1 个重复键**：2015 年 4 月 Lexus 出现两行，分别是 `73` 和 `1`。不要自动丢一行，先展示，再按业务规则决定去重、合并或保留。
3. **model 表有 1274 个重复键**，这不一定是错误：一个月里一个品牌有多款车型，所以 `Year/Month/Make` 本来就是一对多。真正的检查是：`Year/Month/Make/Model` 是否唯一、以及合并前是否已经确认粒度。
4. **model 表的 `Make` 带尾部空格**，例如 `'Volkswagen '`。直接按原始 `Make` 合并会得到 0 行，必须先归一化。

验证一下直接合并会怎样：

```python
print(make.merge(model, on=['Year', 'Month', 'Make'], how='inner').shape)
```

预期输出：

```text
(0, 8)
```

这不是“没有数据”，而是键不匹配。**Agent 如果只说“合并成功”或“0 行”，都不算完成；它必须解释为什么 0 行。**

### 1.3 先归一化键，再决定合并粒度

```python
make_clean = (
    make.assign(Make=make['Make'].str.strip())
    .dropna(subset=['Make'])
    .groupby(['Year', 'Month', 'Make'], as_index=False)['Quantity'].sum()
)

model_clean = (
    model.assign(Make=model['Make'].str.strip())
    .groupby(['Year', 'Month', 'Make'], as_index=False)['Quantity'].sum()
)

print(make_clean.shape)
print(model_clean.shape)
```

预期输出：

```text
(4366, 4)
(1419, 4)
```

这里做了三个明确决定，写进报告：

- `str.strip()` 去掉品牌名尾部空格；
- 品牌缺失的 10 行先单独检查，示例中不进入按品牌合并，因为合并键不能是 `NaN`；
- 两张表都聚合到 `Year/Month/Make` 这一层，再用 `Quantity` 比较；`Pct` 不是本次比较目标。

如果你选择去重而不是求和，就用 `drop_duplicates()`，但要说明保留哪一行、为什么。**不要让 Agent 替你偷偷选择。**

### 1.4 merge、concat 和连接类型

`merge` 是按连接键横向匹配，`concat` 是纵向堆叠（或按列拼接），两者不能互相替代。

```python
stacked = pd.concat(
    [make.head(2), model.head(2)],
    ignore_index=True,
    join='inner'
)
print(stacked.to_string(index=False))
```

预期输出：

```text
 Year  Month        Make  Quantity  Pct
 2007      1      Toyota      2884 22.7
 2007      1  Volkswagen      2521 19.9
 2007      1 Volkswagen       1267 10.0
 2007      1     Toyota        819  6.5
```

`concat` 只是把行放到一起，不会因为 `Make` 相同就匹配；`axis=1` 横向拼接更危险，只在行顺序和行数完全对应时才可用，本课不推荐。

连接类型要逐个验证：

```python
key = ['Year', 'Month', 'Make']

for how in ['inner', 'left', 'right', 'outer']:
    merged = make_clean.merge(
        model_clean,
        on=key,
        how=how,
        suffixes=('_make', '_model')
    )
    print(how, merged.shape)
```

预期输出：

```text
inner (1419, 5)
left (4366, 5)
right (1419, 5)
outer (4366, 5)
```

解释：

- `inner`：只保留两张表都有的品牌月份，1419 行；
- `left`：保留 make 表全部 4366 个品牌月份，车型数量缺失的部分就是 `NaN`；
- `right`：保留 model 表全部 1419 行；
- `outer`：保留两边全部键。本例模型表键全部落在 make 表里，所以 outer 和 left 相同。

不要背结论，要看数据：右连接和全连接为什么相同，因为本数据的 model 键是 make 键的子集。换一组数据结论可能不同。

### 1.5 比较按品牌和按车型汇总是否一致

```python
merged = make_clean.merge(
    model_clean,
    on=key,
    how='left',
    suffixes=('_make', '_model')
)

print(merged.shape)
print('model 缺失:', int(merged['Quantity_model'].isna().sum()))

overlap = merged.dropna(subset=['Quantity_model'])
print('重叠行:', overlap.shape[0])
print('完全相等:', int((overlap['Quantity_make'] == overlap['Quantity_model']).sum()))
```

预期输出：

```text
(4366, 5)
model 缺失: 2947
重叠行: 1419
完全相等: 28
```

1419 个重叠品牌月份里只有 28 个完全相等，不能认为两张表一致。再看品牌总量：

```python
brand_compare = pd.concat(
    [
        make_clean.groupby('Make')['Quantity'].sum(),
        model_clean.groupby('Make')['Quantity'].sum()
    ],
    axis=1,
    keys=['by_make', 'by_model']
).fillna(0)

print(brand_compare.head(10).to_string())
```

预期输出：

```text
              by_make  by_model
Make
Volkswagen     206669  170687.0
Toyota         168177  134656.0
Volvo           99937   71558.0
Ford            99713   57763.0
BMW             73315   22965.0
Audi            70475   28691.0
```

结论：

- make 表有 65 个品牌，车型表只覆盖 22 个品牌；
- 即使品牌相同，车型表汇总总量也明显偏低，例如 Volkswagen 少约 17.4%，Audi 少约 59.3%；
- 因此“按品牌汇总”和“按车型汇总”不一致，可能来自车型表覆盖范围、口径或过滤条件不同。

**不要为了让两张表“看起来一致”就去改数据。报告应该写：哪张表覆盖哪些品牌、用了哪个口径、差异是什么。**

### 1.6 第 1 节课验收

- [ ] 能在合并前输出 shape、缺失值、键重复数和 `Make` 原始值
- [ ] 能解释为什么直接合并得到 0 行
- [ ] 能说出 `merge` 和 `concat` 的区别
- [ ] 能用 `inner/left/right/outer` 跑出预期 shape，并解释差异
- [ ] 能说出品牌汇总和车型汇总不一致的具体证据
- [ ] 原始数据未被修改，重复键和缺失品牌已报告

## 2. 第 2 节课：分组、透视与聚合（45 分钟）

本节课使用：

`data/06-merge/MarketArrivals.csv`

规模为 10227 行，字段含 `market`、`month`、`year`、`quantity`、`priceMin`、`priceMax`、`priceMod`、`state`、`city`、`date`。

问题：**哪些州、哪些市场、哪些年份到货量高？不同月份的价格和数量是否有明显差异？**

### 2.1 先审计，再分组

```text
请读取 data/06-merge/MarketArrivals.csv，只读，不要修改原文件。
第一步只输出 shape、dtypes、缺失值、前 5 行。
不要画图，不要写文件。
```

预期：`(10227, 10)`，10 个字段都没有缺失，`quantity` 是整数，`month` 是字符串，`year` 是整数。

```python
import pandas as pd

arr = pd.read_csv('data/06-merge/MarketArrivals.csv')

print(arr.shape)
print(arr.isna().sum().sum())
print(arr.dtypes)
print(arr.head())
```

先只跑这一段。输出正常后，再执行下一个分析。

### 2.2 groupby 与 agg

`groupby` 负责“按什么分组”，`agg` 负责“每组算什么”。

```python
state_summary = (
    arr.groupby('state')['quantity']
    .agg(count='count', mean='mean', total='sum')
    .round(1)
    .sort_values('total', ascending=False)
)

print(state_summary.head(6))
```

预期输出：

```text
       count      mean      total
state
MS      4354   94496.7  411438840
KNT      958  108837.5  104266318
GUJ      929   92170.9   85626739
DEL      158  293073.5   46305615
WB       187  167496.7   31321878
MP       309   72765.6   22484570
```

注意：**每组必须同时输出 `count`。** 只看 `total` 会忽略样本量，`mean` 和 `total` 都不该脱离 `count` 解释。

再看市场：

```python
market_summary = (
    arr.groupby('market')['quantity']
    .agg(count='count', mean='mean', total='sum')
    .round(1)
    .sort_values('total', ascending=False)
)

print(market_summary.head(6))
```

预期输出：

```text
              count      mean      total
market
BANGALORE       146  523917.4  76491944
PUNE(MS)        206  288692.1  59470570
LASALGAON(MS)   242  242831.4  58765209
PIMPALGAON(MS)  224  237640.2  53231396
DELHI           158  293073.5  46305615
MAHUVA(GUJ)     210  196970.4  41363778
```

不要只看 `total`：BANGALORE 总到货量最高，但它只有 146 行；LASALGAON(MS) 有 242 行，数据覆盖面更广。

### 2.3 pivot_table：年份 × 月份

```python
pivot = arr.pivot_table(
    index='year',
    columns='month',
    values='quantity',
    aggfunc='sum'
)

print(pivot.shape)
print(pivot.loc[[2005, 2010, 2015]].to_string())
```

预期输出：

```text
(21, 12)
```

年份有 1996 到 2016 共 21 年，月份有 12 个。`pivot_table` 适合把“年份、月份”变成二维表，但行数变多后要确认 shape 是否符合预期。

```python
print('2015 年 1 月:', pivot.loc[2015, 'January'])
print('2015 年 12 月:', pivot.loc[2015, 'December'])
```

预期输出：

```text
2015 年 1 月: 8586582.0
2015 年 12 月: 8404299.0
```

### 2.4 crosstab：州 × 月份

```python
ct = pd.crosstab(
    arr['state'],
    arr['month'],
    values=arr['quantity'],
    aggfunc='mean'
).round(1)

print(ct.shape)
print(ct.loc[['DEL', 'MS'], ['January', 'March']].to_string())
```

预期输出：

```text
(21, 12)
month   January     March
state
DEL    248068.8  323100.9
MS     130425.4  113720.8
```

这里有一个容易踩的坑：**crosstab 的 `mean` 是“每个月先求平均，再对月份平均”，和 groupby 的总体平均值不一定相同。**

```python
print('crosstab 月均再平均:', round(ct.loc['DEL'].mean(), 1))
print('groupby 总体平均:', round(arr[arr['state'] == 'DEL']['quantity'].mean(), 1))
```

预期输出：

```text
crosstab 月均再平均: 293646.1
groupby 总体平均: 293073.5
```

两个结果不同，是因为两种口径对每月的权重不同。写报告时只能选一个口径，并写明公式。

### 2.5 第 2 节课验收

- [ ] 能说出 `groupby`、`agg`、`pivot_table`、`crosstab` 各自适合什么场景
- [ ] 分组结果同时包含 `count`、`mean`、`sum` 或等效样本量
- [ ] `pivot_table` 输出 shape 为 `(21, 12)`，抽查过具体数值
- [ ] 能解释 crosstab 平均值与 groupby 平均值为什么可能不同
- [ ] 每一步都先审计、再执行、再验证，原始数据未修改

## 3. 第 3 节课：迷你项目与结论（45 分钟）

本节课使用：

`data/06-merge/email_campaign_funnel.csv`

规模为 42 行，字段为 `Stage`、`Gender`、`Users`。迷你项目可以复用第 1 节的两张销售表之一，或第 2 节的 `MarketArrivals.csv`。

问题：**邮件营销漏斗的转化率是多少？按性别分组后是否一致？**

### 3.1 先审计漏斗数据

这份数据只有 42 行，仍然要先审计：

```python
import pandas as pd

funnel = pd.read_csv('data/06-merge/email_campaign_funnel.csv')

print(funnel.shape)
print(funnel.dtypes)
print(funnel.groupby('Gender')['Users'].agg(['min', 'max']).to_string())
```

预期输出：

```text
(42, 3)
Stage     object
Gender    object
Users    float64
                min           max
Gender
Female -3.066191e+02  1.422643e+07
Male   -1.492762e+07  8.884954e+02
```

不要直接计算转化率。这份数据有三个明显问题：

1. `Users` 是 `float`，不是整数人数；
2. Male 的主序列是负数，Female 的主序列是正数；
3. 前面每个阶段应该是每个性别 1 行，但 `Stage 18: 5th Purchase` 每个性别有 4 行，共 8 行。

先检查阶段行数：

```python
funnel['stage_no'] = funnel['Stage'].str.extract(r'(\d+)').astype(int)
print(funnel['stage_no'].value_counts().sort_index())
```

预期输出：

```text
1     2
2     2
3     2
...
17    2
18    8
```

**看到“人数为负”不能跳过。** 如果老师确认这是教学数据的符号表示，可以在报告中写明“已对 `Users` 取绝对值近似人数”再计算；如果无法确认，就停下来报告数据问题，不编结论。

### 3.2 计算漏斗转化率

```python
funnel['Users_abs'] = funnel['Users'].abs()

wide = funnel.pivot_table(
    index='stage_no',
    columns='Gender',
    values='Users_abs',
    aggfunc='sum'
)
wide['total'] = wide.sum(axis=1)

print(round(wide.loc[13, 'total'] / wide.loc[1, 'total'] * 100, 2))
print(round(wide.loc[18, 'total'] / wide.loc[1, 'total'] * 100, 2))
```

预期输出：

```text
10.58
0.56
```

解释口径：

- 以 Stage 01 Browsers 为 100%；
- 到 Stage 13 Payment Successful 的整体转化率约 10.58%；
- 到 Stage 18 5th Purchase 的整体转化率约 0.56%。

如果看相邻阶段转化，就改成：

```python
step_rate = (wide['total'] / wide['total'].shift(1) * 100).round(2)
print(step_rate)
```

报告里必须写清：**用累计转化率，还是相邻阶段转化率；用原值，还是绝对值。** 不能让读者猜。

### 3.3 按性别分组比较

```python
gender_rate = wide.loc[13, ['Male', 'Female']] / wide.loc[1, ['Male', 'Female']] * 100
print(gender_rate.round(2))
```

预期输出：

```text
Gender
Male      9.98
Female   11.20
```

在绝对值近似口径下，女性到 Payment Successful 的转化率约 11.20%，男性约 9.98%。报告要写：这只是本数据的一个近似结论，原始数据含负数，不能直接当作真实营销系统的人数。

### 3.4 迷你报告结构

在 `projects/<姓名>/mini_project/` 下完成：

```text
projects/<姓名>/mini_project/
  README.md
  scripts/
  output/
```

README 至少写四段：

1. **数据来源**：四张数据表来自 `data/06-merge/`，只读使用；
2. **指标口径**：转化率的分母、分子、是否取绝对值；
3. **结果**：整体转化率和性别差异；
4. **局限**：负数异常、Stage 18 行数异常、样本或口径限制。

迷你项目可以加一个可选分析：复用第 1 节或第 2 节的一张表，输出 1 个分组结论。**不要为了“多表整合”就把没有业务键的表强行 merge。** 漏斗和汽车销售没有共同键，不能因为两句话里都出现“数据”就合并。

自己动手时让 DSH 审查：

```text
请审查 projects/<姓名>/mini_project：
1. 是否报告 funnel 的负数与 Stage 18 行数异常；
2. 是否写清转化率口径（累计还是相邻阶段、原值还是绝对值）；
3. 分组结论是否带样本量或行数；
4. 是否把没有业务键的两张表强行合并；
5. 给出修改建议，不修改原数据。
```

### 3.5 第 3 节课验收

- [ ] 能输出 42 行数据的审计结果，并指出负数与 Stage 18 异常
- [ ] 能解释漏斗转化率的分母和分子
- [ ] 按性别分组表完整，结论注明口径
- [ ] 迷你报告包含数据来源、指标、结果和局限
- [ ] 没有修改原始数据，脚本可复现

## 4. 本周验证清单

- [ ] 第 1 节课验收全部通过
- [ ] 第 2 节课验收全部通过
- [ ] 第 3 节课验收全部通过
- [ ] 每次使用 DSH 都先审计划、检查命令、小步执行、验证结果
- [ ] 合并前检查键的唯一性、缺失和重复，并保留检查输出
- [ ] 能区分 `merge`、`concat`、`groupby`、`pivot_table`、`crosstab` 的用途
- [ ] 能解释按品牌汇总和按车型汇总为什么不一致
- [ ] 能解释漏斗负数为什么不能直接计算转化率
- [ ] 迷你报告结论都能指出“哪张表、哪一列、怎么算的”
- [ ] 原始 `data/` 未修改，代码只写入 `projects/<姓名>/`

## 5. 常见错误与坑

| 现象 | 原因 | 处理 |
|---|---|---|
| merge 直接得到 0 行 | 品牌名有空格或大小写不一致 | 合并前用 `str.strip()` 等归一化，并显示原始值 |
| merge 后行数暴涨 | 键不唯一，一对多展开 | 先查重复键，确认合并粒度 |
| 合并后出现大量空值 | 表覆盖面不同或 how 选错 | 报告未匹配数量，检查 left/right/outer |
| 缺品牌的行被悄悄删掉 | Agent 用 `dropna` 后没说明 | 先输出缺失行，再说明处理规则 |
| 把 concat 当 merge 用 | 不理解纵向堆叠和横向匹配的区别 | concat 只堆行；按键匹配用 merge |
| 分组结果没有 count | 只看 mean 或 sum | 每组必须输出样本量 |
| crosstab 平均值和 groupby 平均值不同 | 两种口径权重不同 | 报告写明公式，不混用 |
| 漏斗人数是负数还直接算转化率 | 没有先审计 | 先报告异常，确认口径后再算 |
| Stage 按字符串排序 | `Stage 10` 会排在 `Stage 2` 前 | 提取阶段编号后排序 |
| 不同领域数据强行合并 | 没有共同业务键 | 不合并，分别写结论 |
| 相信 Agent 说“合并成功” | 只看命令没报错 | 要求输出键检查、shape、空值、未匹配 |
| 原始数据被覆盖 | 工作区选错或提示词没写边界 | 数据目录只读，代码写入 `projects/<姓名>/` |

## 6. 作业

1. 在 `projects/<姓名>/mini_project/` 完成第 3 节课的迷你报告，README 写清数据来源、指标口径和 3 个步骤。
2. 保存第 1 节课的合并审计脚本和输出，至少包含：键重复数、缺失值、`Make` 原始值、四种连接类型 shape、品牌汇总差异。
3. 用 200 字以内回答：按品牌汇总和按车型汇总为什么不一致？funnel 数据里哪些问题让你不能直接相信计算结果？

## 评分要点

| 项目 | 要求 |
|---|---|
| 合并审计 | 能检查键唯一性、缺失、重复，能解释未匹配 |
| 连接类型 | 能说清 `inner/left/right/outer` 的差异 |
| 分组聚合 | 会使用 `groupby`、`agg`、`pivot_table`、`crosstab`，结果带样本量 |
| 漏斗口径 | 能识别负数异常，写明转化率公式 |
| 迷你报告 | 有数据来源、结果、结论和局限，代码可复现 |
| 安全 | 原始数据未修改，命令先检查，每步有验证 |
