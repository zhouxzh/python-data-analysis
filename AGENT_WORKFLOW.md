# Agent 工作规范

本规范约束 Agent 在本仓库中的日常修改、验证和部署行为。目标是：改文档不自动部署，部署必须由用户明确下令。

## 1. 默认行为：改完就停

- 每次只做用户当前要求的最小改动，不顺手重构无关文件。
- 修改完文档、脚本或数据后，默认**不构建、不 push、不部署 GitHub Pages**。
- 只有用户明确说“push”“推送”“构建网页”“部署”“发布到 Pages”等命令时，才执行构建和推送。
- 没有明确部署命令时，最终回复只说明改了什么、本地验证了什么。

## 2. 修改文件规范

- 先读文件再修改，优先使用 `edit`，整文件替换用 `write`。
- 不删除、不 revert 用户已有改动。
- 不批量格式化无关文件，不制造无意义的 diff。
- 引用的数据、价格、来源要写清，拿不准就写明“以官网为准”。

### 2.1 命名规范

- 目录和文件名字要让人类一眼看懂具体内容，禁止只用 `week01` 这类只有机器才懂的编号。
- `scripts/` 子目录结构与 `data/` 同名：`01-agent`、`02-python`、`03-pandas`、`04-viz`、`05-cleaning-merge`。
- 每个脚本目录里“一个例子一个文件”，文件用具体主题命名：`scripts/01-agent/02-read-airbnb.py`。
- 不要把多个数据集、多个练习都塞进一个 `.py` 文件，方便按课单独打开讲解。
- 结果目录与脚本目录同名，结果文件用脚本名 + `-result.txt`：`results/01-agent/02-read-airbnb-result.txt`。
- 编号只用于排序，主题词才是名字的主体；不能为了少写几个字把人类可读性省掉。

### 2.2 代码块、脚本与数据一一对应

- 教程正文中的每个 Python 代码块都必须对应一个独立脚本文件，一个文件只放一个代码块；代码块需要依赖前文变量时，脚本内只补最少的可运行前置代码。
- 文档中出现的程序必须写明对应脚本的 GitHub 链接；同一处要写出该脚本读取的数据文件。
- 文档、脚本、数据文件必须一一对应：看教程时能直接跳到脚本和原始数据，脚本里的路径必须与教程写的数据路径一致。
- 链接必须是 GitHub 仓库链接，不能写本地路径；部署到 GitHub Pages 后使用可点击超链接，链接文本要明确指出是“脚本”还是“数据”。
- 仓库固定前缀：`https://github.com/zhouxzh/python-data-analysis/blob/main/`。

### 2.3 概念完整性与写作插件

- 写教程章节时，先把本节必须讲清的核心概念列出来；基础概念不能跳过。例如 Python 基础必须先完整介绍数据类型，再进入具体语法。
- 写完或大改任何 `.md` 教程章节后，必须调用 `writing_audit`，课程章节用 `profile="notes"` 或 `profile="unknown"`。
- `writing_audit` 报告的 `HIGH`、`MEDIUM` 必须处理；`LOW` 若不适用于 Markdown 课程文本，要说明保留原因。
- 润色或改写已有章节时，用 `writing_audit(original=修改前原文)` 检查数字、代码输出、引用和证据是否被改动。
- 修改最终交付物、提交说明或发布文本时，用 `writing_delivery_audit` 检查被否决方案和修改过程残留是否泄漏。
- 完成后在总结里写明已运行哪个写作插件工具，以及 HIGH/MEDIUM 的处理结果。

### 2.4 写作纪律清单

写正文前先加载 `writing_rules`，完成后用 `writing_audit` 复查。以下规则直接约束所有教程正文和代码说明：

- 不得出现修改过程残留：`revised`、`本轮`、`本次修改`、`投稿前`、`待补齐`、`审稿人要求` 等。
- 不得写自黑免责和空泛套话；结论要有数字、代码输出或可指认数据作证据。
- 不用“不是 X 而是 Y”式对仗句；不堆三连排比、绝对化定义和抽象副词。
- 数字、样本量、代码输出、文件路径、证据状态词不得在润色时改动；`reported/observed/measured` 不能悄悄变成事实陈述。
- 中文平均句长目标不超过 25 字，一句只承担一个判断；长句拆短，短句按论证合并。
- 冒号标题必须前后并列或递进，否则改成空格或更具体标题；避免为整齐而滥用冒号。
- 段落按“一个完整论证单元”切分，避免大量一句成段、机械整齐或明显拥塞。
- 课程笔记因代码块和超链接行造成的 LOW 可保留，但要在总结里说明原因。

## 3. 课程总体要求

- 课程名称：Vibe Programming × Python 数据分析。
- 学制：8 周，每周 3 节课，每节 45 分钟。
- 每周同一晚上上课，整体按“演示、练习、复盘”组织。
- 教学对象：大学生或非计算机专业学生，说明要具体，不能默认读者懂术语。
- 写作语气：教授式、直接、安全第一，不写空话和鼓励式废话。
- 语言：正文和代码注释以中文为主，专有名词保留英文原词。

### 8 周主题

| 周 | 主题 | 主数据领域 |
|---|---|---|
| 01 | Agent 认知、环境与第一个数据问题 | 纽约 Airbnb |
| 02 | Python 编程基础 | 金融、零售、医疗 |
| 03 | pandas 基础 | 电商、教育、汽车 |
| 04 | 数据可视化 | 钻石、人口、能源、公共健康 |
| 05 | 数据清洗、合并与分组聚合 | 汽车、电信、挪威汽车销售 |
| 06 | 第一个预测模型（分类） | 信贷、客户流失 |
| 07 | 回归模型与业务解释 | 房地产 |
| 08 | 结课项目与展示 | 学生从前 7 周数据中选题 |

## 4. 每周文档结构

文件名统一为：

```text
01-认识Agent与第一个数据问题.md
02-Python编程基础.md
03-pandas基础.md
04-数据可视化.md
05-数据清洗与分组聚合.md
06-第一个预测模型.md
07-回归模型与业务解释.md
08-结课项目与展示.md
```

每个周文档至少包含：

1. `# Week N：主题`
2. 本章导读：时长、数据、你将学到、本周产出
3. `## 1. 第 1 节课：...（45 分钟）`
4. `## 2. 第 2 节课：...（45 分钟）`
5. `## 3. 第 3 节课：...（45 分钟）`
6. `## 4. 本周验证清单`
7. `## 5. 常见错误与坑`
8. `## 6. 作业`
9. `## 评分要点`

## 5. 每节课内部结构

每节 45 分钟固定拆分：

- 演示：一个最小闭环
- 练习：追问、修改
- 复盘：验收

每节课必须出现一次完整的 vibe loop：

```text
定义问题 → 要求最小版本 → 运行 → 反馈 → 追问 → 验证
```

每节课结束前必须有 `### N.x 第 x 节课验收`，写成可勾选清单。

DSH 安全使用流程必须反复出现：

```text
先说清目标和验收标准 → 先审 Agent 计划 → 看不懂就解释或暂停 →
执行最小一步 → 检查输出和文件改动 → 保存代码并写结论
```

## 6. 数据要求

数据统一放在：

```text
data/01-agent/
data/02-python/
data/03-pandas/
data/04-cleaning/
data/05-eda-viz/
data/06-merge/
data/07-modeling/
data/08-final/
```

规则：

- 每周使用不同领域数据，每节课尽量用不同类型数据。
- 原始数据只读，不修改、不删除。
- 清洗、分析、图表输出必须写入 `projects/<姓名>/`，不覆盖原始数据。
- 每个数据周目录写 `README.md`，说明来源、规模、字段、许可和课堂问题。
- 下载优先使用 HuggingFace 国内镜像 `hf-mirror.com` 或 GitHub `selva86/datasets`。
- GitHub 资料检索、仓库浏览、`raw` 文件下载统一通过 Clash 代理 `http://127.0.0.1:7890`。
- HuggingFace 模型、数据集和文件统一走 `hf-mirror.com` 镜像。

## 7. 代码与图表要求

- 代码必须能从仓库根目录直接运行。
- 路径使用相对路径，例如 `data/03-pandas/College.csv`。
- Week 2 只用 Python 标准库 `csv`、`statistics`、`math`，不用 pandas。
- Week 3 起使用 pandas；Week 4/5 使用 pandas + matplotlib；Week 6/7 使用 pandas + scikit-learn。
- 建模代码必须设置 `random_state`，先切分训练/测试集，避免数据泄漏。
- 统计结果必须带样本量；模型结果必须带评估指标和简单基线。
- 图表必须有标题、轴标签、图例或必要说明，不能只给裸图。
- Mermaid 图只放必要的流程，短图优先，不要用长流程图占满页面。
- 截图必须来自真实界面或真实数据，不使用虚构占位图。

## 8. 同步更新范围

修改某一周主题时，需要检查并同步：

- 对应周文档 `0X-*.md`
- `syllabus.md`
- `README.md`
- `scripts/0X-<topic>/` 中的单个示例文件和目录 `README.md`
- `scripts/README.md`
- `results/0X-<topic>/` 中对应的结果文件和图表
- 必要时更新 `prompts/core-prompts.md`、`dsh/harness-playbook.md`
- 数据目录 `data/0X-*/README.md`

没有要求时不改无关文件。

## 9. 本地验证规范

- Python 脚本改动：运行对应脚本，例如 `python scripts/01-agent/02-read-airbnb.py`。
- 文档改动：需要时只运行 `npm run docs:build`，不自动推送。
- 浏览器、Mermaid、图片检查只在用户明确要求时做。
- 临时验证脚本用后删除，不留在仓库里。

## 10. 构建与推送触发词

以下表达视为明确部署命令：

- `push`
- `推送`
- `构建网页`
- `构建 GitHub Pages`
- `部署`
- `发布`
- `publish`

没有这些词，就不要构建或推送。

## 11. 标准部署命令

从仓库根目录运行：

```powershell
pwsh -File scripts/deploy-github-pages.ps1 -Message "描述本次改动"
```

只构建、不提交推送：

```powershell
pwsh -File scripts/deploy-github-pages.ps1 -Message "本地构建检查" -SkipPush
```

已有构建产物，只提交并推送：

```powershell
pwsh -File scripts/deploy-github-pages.ps1 -Message "描述本次改动" -SkipBuild
```

## 12. 部署脚本做什么

`scripts/deploy-github-pages.ps1` 按顺序执行：

1. 可选：`npm run docs:build`
2. `git diff --check`
3. `git add -A`
4. `git commit -m "<Message>"`
5. `git push origin main`
6. 使用 `gh run watch` 等待并确认 GitHub Actions Pages 部署

## 13. 禁止行为

- 禁止在未得到明确命令时自动 `git push`。
- 禁止用 `git reset --hard`、`git checkout --` 回退未确认的改动。
- 禁止在文档改动后顺手启动替代服务器。
- 禁止删除 `data/` 中的原始数据或用户项目文件。
- 禁止把未经验证的价格、数据规模、模型指标写进正式文档。
