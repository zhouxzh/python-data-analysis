# Week 6：合并、分组与迷你项目

> **本章导读**
> 时长：3 节课，每节 60 分钟
> 数据：`data/nyc_airbnb.csv`、`data/nyc_boroughs.csv`
> 你将学到：用 `merge` 连接两张表，用 `groupby` 做多指标汇总，并完成一个 120 字迷你报告
> 本周产出：`projects/<姓名>/mini_project/`

## 1. 跟着老师做

### 1.1 先确定要回答的问题

```text
行政区的人口、面积和收入，和 Airbnb 平均价格有什么关系？
```

要回答这个问题，单张 Airbnb 表不够，还要把行政区小表合并进来。

### 1.2 发给 DSH 的第一版提示词

```text
请读取 data/nyc_airbnb.csv 和 data/nyc_boroughs.csv。
按行政区合并，输出：
1. 合并后 shape；
2. 哪些行政区只在 Airbnb 表、哪些只在 nyc_boroughs；
3. 合并后是否有空值；
4. 每个行政区的房源数、平均价格、中位价格、人口、面积和收入。
不要修改原文件。
```

### 1.3 合并两张表

```python
import pandas as pd

df = pd.read_csv('data/nyc_airbnb.csv')
info = pd.read_csv('data/nyc_boroughs.csv')

grouped = (
    df.groupby('neighbourhood_group')['price']
    .agg(listings='size', mean_price='mean', median_price='median')
    .round(2)
    .reset_index()
)

merged = grouped.merge(
    info,
    left_on='neighbourhood_group',
    right_on='borough',
    how='left'
)

print('merged shape:', merged.shape)
print('合并后缺失值:', int(merged.isna().sum().sum()))
print(merged.to_string(index=False))
```

预期输出：

```text
merged shape: (5, 8)
合并后缺失值: 0
neighbourhood_group  listings  mean_price  median_price       borough  population_2020  land_area_sqmi  median_household_income_usd
              Bronx      1091       87.50          65.0         Bronx          1472654            42.2                        43000
           Brooklyn     20104      124.38          90.0      Brooklyn          2736074            69.4                        65000
          Manhattan     21661      196.88         150.0     Manhattan          1694251            22.7                       102000
             Queens      5666       99.52          75.0        Queens          2405464           108.7                        72000
      Staten Island       373      114.81          75.0 Staten Island           495747            57.5                        88000
```

### 1.4 查看价格与收入的关系

```python
print(merged[['mean_price', 'population_2020',
              'land_area_sqmi',
              'median_household_income_usd']]
      .corr()['mean_price']
      .round(3))
```

预期输出：

```text
mean_price                     1.000
population_2020                0.013
land_area_sqmi                -0.585
median_household_income_usd    0.802
Name: mean_price, dtype: float64
```

### 1.5 解读

```text
平均价格和中位收入的相关性是 0.802，看起来比较强；
但这里只有 5 个行政区，样本太少，不能当成稳定规律。
Staten Island 只有 373 个房源，样本量也要单独注明。
结论必须写清楚：我用的是哪个指标、每个区有多少房源。
```

## 2. 你自己动手做

1. 新建 `projects/<姓名>/mini_project/`。
2. 在 DSH 中使用 plan mode：

```text
/plan 我要回答：行政区的收入水平与 Airbnb 平均价格是否有关系？
请先调查数据字段，给出：指标定义、合并方式、统计方法、图表类型、验收标准。
先不要改文件。
```

3. 确认方案后，让 DSH 生成代码并运行。
4. 输出：
   - 1 张图；
   - 120 字以内结论；
   - 1 条局限。

自己动手时建议用这个提示词：

```text
请审查我的 mini_project：
1. 合并是否报告了未匹配和空值；
2. 指标是否写清是简单平均还是按房源量加权；
3. 结论是否带样本量和局限；
4. 是否说明只有 5 个行政区的限制；
5. 给出修改后的代码。
```

## 3. 验证清单

- [ ] 合并前后 shape 和未匹配行政区有记录
- [ ] 汇总表包含房源数、平均价格、中位价格和人口/收入
- [ ] 结论写清指标定义和样本量
- [ ] 原文件未修改
- [ ] 迷你报告可复现

## 4. 常见错误与坑

| 现象 | 原因 | 处理 |
|---|---|---|
| merge 后行数变多/变少 | 键不唯一或 how 选错 | 先检查键唯一性 |
| 合并后出现空值 | 某张表缺行政区 | 报告未匹配列表 |
| 简单平均和加权平均混用 | 没写口径 | 报告中明确指标公式 |
| 5 个点当统计规律 | 样本太少 | 写“相关性仅基于 5 个行政区” |
| 小样本行政区被忽略 | Staten Island 只有 373 个房源 | 结论必须带 count |

## 5. 作业

提交 mini project，要求包含：

```text
projects/<姓名>/mini_project/
  README.md
  scripts/
  output/
```

README 写清楚数据来源、指标定义和 3 个步骤。

## 评分要点

| 项目 | 要求 |
|---|---|
| 合并 | 能解释未匹配和空值 |
| 汇总 | 能同时输出多指标，理解加权均值 |
| 项目 | 问题明确、代码可运行、结论 ≤120 字 |
| 局限 | 至少写出 1 条样本或口径局限 |
