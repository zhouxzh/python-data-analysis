# Week 3：pandas 基础

> **本章导读**
> 时长：3 节课，每节 45 分钟
> 数据：`data/03-pandas/olist_orders_45d.csv`、`data/03-pandas/College.csv`、`data/03-pandas/Cars93.csv`
> 你将学到：用 pandas 读取数据、认识 DataFrame、解析日期、筛选排序、新建列、分组聚合和缺失检查
> 本周产出：`projects/<姓名>/` 下的 3 个 pandas 脚本

本周正式学习 pandas。课程用三种不同领域的数据：

- 电商订单：`olist_orders_45d.csv`，5480 行，字段 `purchase_time`、`purchase_date`、`quantity`；
- 美国大学：`College.csv`，777 行，字段含 `Private`、`Apps`、`Accept`、`Enroll`、`Outstate`、`Room.Board`、`PhD`、`Grad.Rate`、`S.F.Ratio`；
- 汽车：`Cars93.csv`，93 行，字段含 `Manufacturer`、`Type`、`Price`、`MPG.city`、`MPG.highway`、`Horsepower`、`Weight`、`Origin`。

pandas 数据分析流程可以概括为一个短流程：

```mermaid
flowchart LR
    A[读取 CSV] --> B[检查 shape / columns / dtypes]
    B --> C[解析日期并检查缺失]
    C --> D[筛选 / 排序 / 新建列]
    D --> E[分组聚合]
    E --> F[核对输出与结论]
```

本周使用 DSH 的固定规则：**先让 Agent 给出计划和命令，看懂了才让它执行；每次只执行最小一步；每步核对输出；原始 `data/` 目录只读。** 不要直接对 DSH 说“做完发我”，更不要在没看到命令时点执行。

## 1. 第 1 节课：认识 DataFrame（45 分钟）

第一节课处理电商订单数据：`data/03-pandas/olist_orders_45d.csv`。目标是学会把 CSV 读进 pandas，并能回答三个问题：这个表有多少行多少列？每列是什么类型？日期列是不是真正的日期？

### 1.1 先看 DSH 的计划

用 DSH 之前，先要求它给计划，不要让它直接执行：

```text
请先给我一个执行计划，先不要执行。
目标：读取 data/03-pandas/olist_orders_45d.csv，认识这张表。
要求：
1. 不修改原文件，data/ 目录只读；
2. 先输出 shape、columns、dtypes；
3. 再输出 head、tail、describe、info；
4. 最后用 pandas 解析日期并验证类型；
5. 每一步先说明命令在做什么，再运行。
```

看到计划后逐条检查：路径是不是 `data/03-pandas/olist_orders_45d.csv`？命令是不是只读？每一步的输出能不能验证？计划里有一行看不懂，就让它解释；解释后仍看不懂，先找老师，不点执行。

### 1.2 读取数据并查看结构

第一小步只做读取和结构检查：

```python
import pandas as pd

df = pd.read_csv('data/03-pandas/olist_orders_45d.csv')

print('行数与列数:', df.shape)
print()
print('列名:', df.columns.tolist())
print()
print('每列类型:')
print(df.dtypes)
```

预期输出：

```text
行数与列数: (5480, 3)
列名: ['purchase_time', 'purchase_date', 'quantity']
每列类型:
purchase_time    object
purchase_date    object
quantity          int64
dtype: object
```

要解释清楚：

- `shape` 返回 `(行数, 列数)`，本表是 `(5480, 3)`；
- `columns` 是列名；
- `dtypes` 是每列的数据类型；
- `object` 表示文本类型，`int64` 表示整数。`purchase_time`、`purchase_date` 目前还是文本，不是日期；
- 最后一行 `dtype: object` 是 pandas 打印这个“类型列表”时附带的格式，不是某个数据列的类型。

### 1.3 查看数据头尾与统计信息

结构检查通过后，再分步看内容：

```python
print(df.head())
print()
print(df.tail(3))
print()
print(df.describe())
print()
df.info()
```

预期输出要点：

```text
      purchase_time purchase_date  quantity
0 2017-05-16 13:10:30   2017-05-16         5
1 2017-05-16 19:41:10   2017-05-16         3
2 2017-05-19 18:53:40   2017-05-19         2

count    5480.000000
mean        7.495255
std         4.029637
min         1.000000
25%         4.000000
50%         8.000000
75%        11.000000
max        14.000000
```

规则：

- `head()` 看开头，`tail()` 看结尾；
- `describe()` 默认只统计数值列，所以这里只有 `quantity` 的 `count`、`mean`、`std`、四分位数和 `min`/`max`；
- `info()` 会把每一列的列名、非空数量、类型一起列出来，比只看 `head()` 可靠；
- `info()` 里的 `Memory usage` 会随 pandas 版本变化，关键是确认列名、非空数量和 dtypes。

### 1.4 解析日期

`purchase_time` 看起来像时间，但 dtypes 是 `object`。要让 pandas 把它变成真正的日期，用 `pd.to_datetime`：

```python
df['purchase_time'] = pd.to_datetime(df['purchase_time'], errors='coerce')
df['purchase_date'] = pd.to_datetime(df['purchase_date'], errors='coerce')

print(df.dtypes)
print()
print(df[['purchase_time', 'purchase_date', 'quantity']].head(3))
```

预期输出：

```text
purchase_time    datetime64[ns]
purchase_date    datetime64[ns]
quantity                  int64
dtype: object

      purchase_time purchase_date  quantity
2017-05-16 13:10:30    2017-05-16         5
2017-05-16 19:41:10    2017-05-16         3
2017-05-19 18:53:40    2017-05-19         2
```

`errors='coerce'` 的含义是：能解析的转成日期，解析不了的变成缺失值 `NaT`。不要省略它，否则一条脏数据就可能让整列报错。解析后要再看一次 `dtypes` 和缺失数，确认转换成功。

### 1.5 让 DSH 整理成脚本

自己先做完一遍后，再让 DSH 整理脚本：

```text
请读取 data/03-pandas/olist_orders_45d.csv，不修改原文件。
把第 1 节课的步骤整理成 projects/<姓名>/week03_orders.py，要求：
1. 用 pandas 的 read_csv 读取；
2. 输出 shape、columns、dtypes、head、tail、describe、info；
3. 用 to_datetime(errors='coerce') 解析 purchase_time 和 purchase_date；
4. 先告诉我你会创建哪个文件、会运行哪些命令，确认后再执行。
```

执行前先看它的计划，确认脚本只写到 `projects/<姓名>/`，不会碰 `data/`。运行后检查两件事：输出和上课讲的一致；脚本里的路径是 `data/03-pandas/olist_orders_45d.csv`。

### 1.6 第 1 节课验收

下课前至少完成：

- [ ] 能说出 `shape` 为什么是 `(5480, 3)`
- [ ] 能区分 `columns`、`dtypes`、`head`、`tail` 的用途
- [ ] 能解释 `describe()` 默认只统计数值列
- [ ] `purchase_time`、`purchase_date` 已用 `to_datetime(errors='coerce')` 转成 `datetime64[ns]`
- [ ] 用 DSH 前先看计划，计划里没有修改 `data/` 的命令
- [ ] `projects/<姓名>/week03_orders.py` 可运行

## 2. 第 2 节课：筛选、排序与列操作（45 分钟）

第二节课处理美国大学数据：`data/03-pandas/College.csv`，777 行 18 列。主要字段有 `Private`、`Apps`、`Accept`、`Enroll`、`Outstate`、`Room.Board`、`PhD`、`Grad.Rate`、`S.F.Ratio`。目标是学会用位置和条件挑出需要的行，按指标排序，并新建计算列。

同样先走 DSH 安全流程：先让 Agent 给计划，计划里必须写明读取 `data/03-pandas/College.csv`、不修改原文件、分小步运行、每步展示输出，确认后再执行。

### 2.1 读取数据

```python
import pandas as pd

college = pd.read_csv('data/03-pandas/College.csv')

print(college.shape)
print(college.columns.tolist())
```

预期输出要点：

```text
(777, 18)
['Private', 'Apps', 'Accept', 'Enroll', 'Top10perc', 'Top25perc', 'F.Undergrad',
 'P.Undergrad', 'Outstate', 'Room.Board', 'Books', 'Personal', 'PhD', 'Terminal',
 'S.F.Ratio', 'perc.alumni', 'Expend', 'Grad.Rate']
```

### 2.2 `loc` 和 `iloc`

`loc` 按标签或条件取，`iloc` 按整数位置取：

```python
# loc：取指定的行标签和列
print(college.loc[[0, 1], ['Private', 'Apps', 'Accept', 'Enroll']])
print()

# loc：行里放一个布尔条件
print(college.loc[college['Private'] == 'Yes', ['Private', 'Apps', 'Outstate']].head(3))
print()

# iloc：取前 3 行、前 5 列
print(college.iloc[0:3, 0:5])
```

预期输出（节选）：

```text
  Private  Apps  Accept  Enroll
    Yes  1660    1232     721
    Yes  2186    1924     512

  Private  Apps  Outstate
    Yes  1660      7440
    Yes  2186     12280
    Yes  1428     11250

  Private  Apps  Accept  Enroll  Top10perc
    Yes  1660    1232     721         23
    Yes  2186    1924     512         16
    Yes  1428    1097     336         22
```

记忆方法：`loc` 里的内容是“行名或条件 + 列名”，`iloc` 里全是整数位置。默认索引是 `0, 1, 2, ...`，所以这一份数据里两者看起来很像，但含义不同。

### 2.3 条件筛选

条件筛选就是给 DataFrame 一个布尔条件，pandas 只保留条件为 `True` 的行：

```python
private = college[college['Private'] == 'Yes']
public = college[college['Private'] == 'No']

print('私立学校数量:', private.shape[0])
print('公立学校数量:', public.shape[0])
print()

big_school = college[college['Apps'] > 10000]
print('申请人数超过 10000 的学校:')
print(big_school[['Private', 'Apps', 'Accept']].head(5))
print()

high_accept = college[college['Accept'] / college['Apps'] > 0.7]
print('录取率高于 0.7 的学校数量:', high_accept.shape[0])
```

预期输出要点：

```text
私立学校数量: 565
公立学校数量: 212

申请人数超过 10000 的学校:
  Private   Apps  Accept
     No   12809   10308
    Yes   20192   13007
    Yes   12586    3239
...

录取率高于 0.7 的学校数量: 544
```

注意：`college['Apps'] > 10000` 本身是一个布尔 Series，里面每个值是 `True` 或 `False`；把它放进 `college[...]`，pandas 就按它过滤行。

### 2.4 `sort_values` 排序

```python
top_apps = college.sort_values('Apps', ascending=False)

print('申请人数最多的 3 所学校:')
print(top_apps[['Private', 'Apps', 'Accept', 'Enroll']].head(3))
```

预期输出：

```text
申请人数最多的 3 所学校:
  Private   Apps  Accept  Enroll
     No   48094   26330    4520
     No   21804   18744    5874
    Yes   20192   13007    3810
```

默认 `ascending=True` 是从小到大；要得到最大的前几名，必须写 `ascending=False`。`sort_values` 不会改原表，要保存结果就赋给新变量。

### 2.5 新建列

新建列用 `df['新列名'] = 计算结果`：

```python
college['AcceptRate'] = (college['Accept'] / college['Apps'] * 100).round(1)
college['TotalCost'] = college['Outstate'] + college['Room.Board']

print(college[['Private', 'Apps', 'Accept', 'AcceptRate', 'Outstate', 'Room.Board', 'TotalCost']].head(5))
```

输出要点：`AcceptRate` 是录取率百分比，`TotalCost` 是州外学费加住宿费。先写清楚计算口径，再新建列；新列必须能被同行的人用一句话解释。

给 DSH 的协作提示词：

```text
请读取 data/03-pandas/College.csv，不修改原文件。
生成 projects/<姓名>/week03_college.py，要求：
1. 只使用 pandas；
2. 分别用 loc、iloc 展示取数；
3. 筛选 Private == 'Yes'，并按 Apps 降序取前 10 名；
4. 新建 AcceptRate 和 TotalCost 两列；
5. 先给计划和要运行的命令，确认后再执行。
```

### 2.6 第 2 节课验收

- [ ] 能解释 `loc` 和 `iloc` 的区别
- [ ] 能用布尔条件筛选行，并说明 `private.shape[0]` 的含义
- [ ] `sort_values('Apps', ascending=False)` 能找出申请人数最多的学校
- [ ] 能新建 `AcceptRate`、`TotalCost` 两列并解释口径
- [ ] 用 DSH 前先看计划，确认脚本写入 `projects/<姓名>/`
- [ ] `projects/<姓名>/week03_college.py` 可运行

## 3. 第 3 节课：分组与聚合（45 分钟）

第三节课处理汽车数据：`data/03-pandas/Cars93.csv`，93 行 27 列。主要字段有 `Manufacturer`、`Type`、`Price`、`MPG.city`、`MPG.highway`、`Horsepower`、`Weight`、`Origin`。目标是按类别汇总，让一张 93 行的表变成可比较的汇总表，并学会检查缺失。

继续遵守 DSH 安全流程：先要计划，计划里写明读取 `data/03-pandas/Cars93.csv`、`data/` 只读、先 `value_counts` 再 `groupby`、最后检查缺失，确认后再执行。

### 3.1 分类列计数：`value_counts`

```python
import pandas as pd

cars = pd.read_csv('data/03-pandas/Cars93.csv')

print(cars.shape)
print()
print('车型分布:')
print(cars['Type'].value_counts())
print()
print('产地分布:')
print(cars['Origin'].value_counts())
print()
print('品牌出现次数前 3:')
print(cars['Manufacturer'].value_counts().head(3))
```

预期输出要点：

```text
(93, 27)

车型分布:
Midsize    22
Small      21
Compact    16
Sporty     14
Large      11
Van         9

产地分布:
USA        48
non-USA    45

品牌出现次数前 3:
Chevrolet    8
Ford         8
Dodge        6
```

`value_counts()` 返回的是“每个取值出现多少次”。它是分类列最常用的第一个检查，能立刻发现数据里有哪些类别、样本量是否均衡。

### 3.2 `groupby` 分组

```python
type_mean = cars.groupby('Type')[['Price', 'MPG.city', 'MPG.highway']].mean().round(1)

print(type_mean)
```

预期输出：

```text
         Price  MPG.city  MPG.highway
Type
Compact   18.2      22.7         29.9
Large     24.3      18.4         26.7
Midsize   27.2      19.5         26.7
Small     10.2      29.9         35.5
Sporty    19.4      21.8         28.8
Van       19.1      17.0         21.9
```

结构拆开看：`groupby('Type')` 按车型分组，`[['Price', 'MPG.city', 'MPG.highway']]` 选要计算的列，`.mean()` 算平均值，`.round(1)` 保留一位小数。分组之后必须接一个聚合操作，否则拿到的只是一个分组对象，看不到汇总表。

### 3.3 `agg` 同时算多个指标

一个分组里往往既要样本量，又要平均、最小、最大，这时用 `agg`：

```python
summary = cars.groupby('Type')[['Price', 'MPG.city']].agg(
    ['count', 'mean', 'min', 'max']
).round(1)

print(summary)
print()
print(summary.sort_values(('Price', 'mean'), ascending=False))
```

`agg(['count', 'mean', 'min', 'max'])` 会为每一列生成四个统计值。对分组结果再次 `sort_values` 时，列名是两层结构，所以要写 `('Price', 'mean')` 这种元组，不能只写 `'Price'`。

给 DSH 的协作提示词：

```text
请读取 data/03-pandas/Cars93.csv，不修改原文件。
生成 projects/<姓名>/week03_cars.py，要求：
1. 只使用 pandas；
2. 用 value_counts 看 Type 和 Origin；
3. 用 groupby 按 Type 汇总 Price、MPG.city、MPG.highway 的平均值；
4. 用 agg 同时输出 count、mean、min、max，并按价格均值降序排序；
5. 先给计划和命令，确认后再执行。
```

### 3.4 简单缺失检查

```python
missing = cars.isna().sum()
print(missing)
print()
print('有缺失的列:')
print(missing[missing > 0])
```

预期输出（节选：只看有缺失的列）：

```text
AirBags           34
Rear.seat.room     2
Luggage.room      11
```

`isna()` 把每个值变成“是不是缺失”的布尔值，`.sum()` 按列求和，所以得到每列缺失数量。缺失检查只做“报告”，不在这一步决定删除或填充。`AirBags` 有 34 个缺失，可能是因为这些车没有安全气囊信息；要先说明它意味着什么，再决定分析时怎么处理。

### 3.5 DSH 审查脚本

脚本完成后，让 DSH 审查而不是直接让它改：

```text
请审查 projects/<姓名>/week03_cars.py，先不要改代码。
1. 分组后是否同时报告样本量；
2. 排序的列名是否写对；
3. 是否报告了缺失值；
4. 每条结论能否指出用了哪个字段、怎么计算；
5. 给出修改建议清单，等我确认后再改。
```

审查建议看一遍后，仍然先要求 DSH 给出具体修改计划，再执行。

### 3.6 第 3 节课验收

- [ ] `value_counts()` 能输出 `Type`、`Origin` 的分布
- [ ] 能解释 `groupby('Type')[['Price', 'MPG.city', 'MPG.highway']].mean()` 每一层在做什么
- [ ] `agg(['count', 'mean', 'min', 'max'])` 输出中包含样本量
- [ ] 分组结果能按 `('Price', 'mean')` 降序排序
- [ ] `isna().sum()` 能报告 `AirBags`、`Rear.seat.room`、`Luggage.room` 的缺失
- [ ] 用 DSH 前先看计划，执行前能说出每条命令在做什么
- [ ] `projects/<姓名>/week03_cars.py` 可运行

## 4. 本周验证清单

- [ ] 能用 pandas 读取三个文件，路径均为 `data/03-pandas/...`
- [ ] 能说出 `shape`、`columns`、`dtypes`、`head`、`tail`、`describe`、`info` 各自的用途
- [ ] 能用 `to_datetime(errors='coerce')` 解析日期，并检查转换后的 dtypes
- [ ] 能区分 `loc` 和 `iloc`，能用布尔条件筛选行
- [ ] 能用 `sort_values` 排序，并用新建列保存计算结果
- [ ] 能用 `groupby`、`agg`、`value_counts` 生成分组汇总
- [ ] 能用 `isna().sum()` 报告缺失，而不是直接填充或删除
- [ ] 每个结论都能指出“用了哪一列、怎么计算、样本量多少”
- [ ] 每次使用 DSH 都先看计划，能解释要运行的命令
- [ ] 原始 `data/` 目录没有被修改，代码只写入 `projects/<姓名>/`

## 5. 常见错误与坑

| 现象 | 原因 | 处理 |
|---|---|---|
| `FileNotFoundError` | 工作区或路径不对 | 先确认当前工作区是课程仓库根目录，路径写 `data/03-pandas/...` |
| `purchase_time` 仍是 `object` | 没有解析日期 | 用 `pd.to_datetime(..., errors='coerce')`，再检查 dtypes |
| 只看 `head()` 就说数据没问题 | head 只显示开头几行 | 同时看 `shape`、`info()`、`isna().sum()` |
| `describe()` 没有分类列 | describe 默认只统计数值列 | 用 `info()`、`value_counts()` 看文本列 |
| `loc` 和 `iloc` 混用报错 | 一个按标签/条件，一个按整数位置 | 先确认索引和需求，再选择取数方式 |
| 筛选后赋值出现 `SettingWithCopyWarning` | 在切片视图上写回 | 先 `.copy()`，再新建列 |
| `groupby` 后直接 `print` 看不到汇总 | 分组对象还没聚合 | 加 `mean()`、`agg()` 或 `value_counts()` |
| 分组结论没写样本量 | 只算了平均值 | 聚合里同时算 `count`，样本太少不下结论 |
| 缺失值被 `mean` 跳过 | pandas 默认忽略 NaN | 先 `isna().sum()`，报告缺失再分析 |
| DSH 说“完成”但你没看到命令 | 没有审查执行过程 | 要求先给计划，逐条解释命令，再执行 |
| DSH 修改了原数据 | 提示词没写边界 | 明确写“data/ 只读，代码写入 projects/<姓名>/” |
| 脚本在别的目录运行失败 | 路径依赖当前目录 | 统一从仓库根目录运行，并核对路径 |

## 6. 作业

1. 保存并运行三个脚本：`projects/<姓名>/week03_orders.py`、`projects/<姓名>/week03_college.py`、`projects/<姓名>/week03_cars.py`。
2. 对订单数据，用 `df['purchase_date'].value_counts().head(3)` 找出订单最多的 3 天，并用中文写一句结论，说明样本量。
3. 对大学数据，比较 `Private == 'Yes'` 和 `Private == 'No'` 的 `Apps` 平均值，并解释为什么分组结论必须同时看样本量。
4. 对汽车数据，按 `Origin` 分组计算 `MPG.city` 的 `mean` 和 `count`，回答“美国车和非美国车的城市油耗谁更低”，并报告 `Cars93.csv` 中有缺失的列。
5. 让 DSH 审查三个脚本，审查时先要计划、不直接修改；最后把 DSH 给出的修改建议写成 3 条你认可和不认可的理由，保存到 `projects/<姓名>/week03_review.md`。

## 评分要点

| 项目 | 要求 |
|---|---|
| DataFrame 基础 | `read_csv`、`shape`、`columns`、`dtypes`、`head`、`tail`、`describe`、`info` 都能使用和解释 |
| 日期解析 | `purchase_time`、`purchase_date` 转 `datetime64[ns]`，使用 `errors='coerce'` |
| 筛选排序 | `loc`、`iloc`、条件筛选、`sort_values`、新建列 |
| 分组聚合 | `groupby`、`agg`、`value_counts`，汇总中包含 `count` |
| 缺失检查 | 用 `isna().sum()` 报告，不静默填充或删除 |
| 结论依据 | 每条结论能指出字段、计算方式和样本量 |
| 安全 | 先看 Agent 计划，能解释每条命令，`data/` 未修改 |
| 交付 | 3 个脚本可运行，审查记录保存在 `projects/<姓名>/week03_review.md` |
