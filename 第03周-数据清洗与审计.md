# Week 3：数据清洗与审计

> **本章导读**
> 时长：3 节课，每节 45 分钟
> 数据：`data/nyc_airbnb.csv`
> 你将学到：先审计再清洗，把清洗步骤写成函数，并识别缺失、异常价格和“0 不等于没数据”这类语义问题
> 本周产出：`projects/<姓名>/cleaning_report.md`

## 1. 跟着老师做

### 1.1 先确定要回答的问题

```text
这份 Airbnb 数据能不能直接做价格分析？
```

直接运行前，必须先回答三个问题：

- 有没有重复行或重复房源 ID？
- 有没有缺失值？
- 价格和日期列是否干净？

### 1.2 发给 DSH 的第一版提示词

```text
请审计 data/nyc_airbnb.csv，不要修改原文件。
输出：
1. shape、dtypes；
2. 重复行数和 id 重复数；
3. 每列缺失数；
4. price 的 min / max / describe；
5. last_review 转成日期后有多少缺失；
6. 列出 3 个必须处理的数据质量问题。
```

### 1.3 数据质量审计

```python
import pandas as pd

df = pd.read_csv('data/nyc_airbnb.csv')

print('shape:', df.shape)
print('重复行数:', df.duplicated().sum())
print('id 重复数:', df['id'].duplicated().sum())
print()
print('缺失值:')
print(df.isna().sum())
print()
print('price describe:')
print(df['price'].describe().round(2))
```

预期输出：

```text
shape: (48895, 16)
重复行数: 0
id 重复数: 0

缺失值:
id                                    0
name                                 16
host_id                               0
host_name                            21
neighbourhood_group                   0
neighbourhood                         0
latitude                              0
longitude                             0
room_type                             0
price                                 0
minimum_nights                        0
number_of_reviews                     0
last_review                       10052
reviews_per_month                 10052
calculated_host_listings_count        0
availability_365                      0

price describe:
count    48895.00
mean       152.72
std        240.15
min          0.00
25%         69.00
50%        106.00
75%        175.00
max      10000.00
```

可以看到四类问题：

1. `name` 缺失 16 行、`host_name` 缺失 21 行。
2. `last_review` 和 `reviews_per_month` 缺失 10052 行，通常是因为该房源没有评论。
3. `price` 有 0 和 10000 这类异常值：`price == 0` 有 11 行，`price > 1000` 有 239 行。
4. `number_of_reviews == 0` 的房源有 10052 行，正好与 `last_review` 缺失对应：**0 不是缺失，而是“没有评论”这一真实状态**。

### 1.4 清洗函数

```python
def clean_airbnb(df):
    cleaned = df.copy()
    cleaned = cleaned.drop_duplicates()
    cleaned['last_review'] = pd.to_datetime(cleaned['last_review'], errors='coerce')
    cleaned['price'] = pd.to_numeric(cleaned['price'], errors='coerce')
    cleaned['anomaly_price'] = (cleaned['price'] <= 0) | (cleaned['price'] > 1000)
    return cleaned


cleaned = clean_airbnb(df)

print('清洗前:', df.shape)
print('清洗后:', cleaned.shape)
print('重复行:', cleaned.duplicated().sum())
print('日期缺失:', cleaned['last_review'].isna().sum())
print('价格异常:', cleaned['anomaly_price'].sum())
```

预期输出：

```text
清洗前: (48895, 16)
清洗后: (48895, 17)
重复行: 0
日期缺失: 10052
价格异常: 250
```

说明：我们先把问题“标记”出来，而不是直接删掉 250 条异常价格。是否删除或修正，必须由分析口径决定。

### 1.5 清洗报告模板

```markdown
# 清洗报告

- 数据：data/nyc_airbnb.csv
- 原始行数：48895
- 清洗后行数：48895
- 重复行：0；id 重复：0
- 缺失：name 16，host_name 21，last_review 10052，reviews_per_month 10052
- 价格异常：price == 0 有 11 行，price > 1000 有 239 行，共 250 行
- 日期：last_review 转 datetime，缺失 10052
- 保留的缺失策略：先观察，不在没依据时强行填充
```

## 2. 你自己动手做

1. 新建 `projects/<姓名>/cleaning_report.md`。
2. 让 DSH 把你的清洗步骤整理成函数 `clean_airbnb(df)`。
3. 分别试 `dropna()` 删除所有缺失和只删除 `name` 缺失，比较清洗后行数和平均价格变化。
4. 用中文解释：为什么清洗策略会影响后面的结论？

自己动手时建议用这个提示词：

```text
请审查我的清洗函数：
1. 是否误删了有价值的数据；
2. last_review 缺失是否应该填充；
3. price == 0 和 price > 1000 是否应该删除；
4. 给出修改后的函数和清洗前后对比表。
```

## 3. 验证清单

- [ ] 原文件没有被修改
- [ ] 清洗前后 shape、重复行、缺失数都能对比
- [ ] 清洗步骤被封装成函数
- [ ] 报告写清了每个删除或填充的理由
- [ ] 价格异常被标记而不是静默删除
- [ ] 脚本可用 `python scripts/week03_practice.py` 运行

## 4. 常见错误与坑

| 现象 | 原因 | 处理 |
|---|---|---|
| 重复行没删掉 | 只看了 head | 使用 `duplicated().sum()`，并检查业务主键 `id` |
| 日期无法解析 | `last_review` 本身缺失 | `to_datetime(errors='coerce')` 后检查 NaT |
| 删掉太多行 | 缺失去重策略太激进 | 先输出缺失分布再决定 |
| 把 `number_of_reviews == 0` 当缺失 | 0 是“没有评论”的真实状态 | 保留 0，和缺失分开报告 |
| 把 0 元价格当正常价格 | 业务上不可能 | 标记为异常并说明处理口径 |
| 填充让均值失真 | 默认 `fillna` | 写清策略并保留原始缺失数 |

## 5. 作业

让 DSH 对同一份数据做“自动审计”，然后人工核对，列出 AI 遗漏的 2 个质量问题，并说明你是如何发现的。

## 评分要点

| 项目 | 要求 |
|---|---|
| 审计 | 能发现缺失、日期、价格异常、无评论状态 |
| 清洗 | 函数可运行，原文件未改 |
| 对比 | 输出清洗前后行数和缺失 |
| 语义 | 能区分 NaN、0 和业务异常 |
