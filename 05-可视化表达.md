# Week 5：EDA 与可视化

> **本章导读**
> 时长：3 节课，每节 45 分钟
> 数据：`data/05-eda-viz/diamonds.csv`、`data/05-eda-viz/midwest.csv`、`data/05-eda-viz/energy_dataset.csv`
> 你将学到：先用 `describe`、`value_counts`、缺失值统计探索数据，再提出具体问题，最后用直方图、箱线图、条形图、散点图和折线图验证问题
> 本周产出：`projects/<姓名>/week5_eda_viz.py`，以及 `projects/<姓名>/week5_plots/` 下的 3 张图

本周不做“先找数据再画图”的无序探索。每节课统一走这个流程：**先读数据、审计规模与字段，再写出具体问题，最后让图表回答问题**。一张图必须能回答一个可指认的问题；说不清问题，就不应该先画图。

三节课安排：

1. 第 1 节课：单变量分布与异常值（`diamonds.csv`）
2. 第 2 节课：分类对比与关系图（`midwest.csv`）
3. 第 3 节课：时间序列与趋势（`energy_dataset.csv`）

每次让 DSH 画图前，先按固定流程做：**理解计划 → 检查命令 → 小步执行 → 验证结果**。DSH 生成的绘图代码必须先看懂再运行；看不懂就让 DSH 逐行解释。`data/05-eda-viz/` 只读，输出只写入 `projects/<姓名>/week5_plots/`。

```mermaid
flowchart LR
    A[读数据：shape、字段、缺失] --> B[提出具体问题]
    B --> C[选匹配的图<br/>直方图/箱线图/条形图/散点图/折线图]
    C --> D[加标题、轴标签、图例、样本量]
    D --> E{图能回答原问题吗}
    E -- "不能" --> B
    E -- "能" --> F[写结论并注明局限]
```

## 1. 第 1 节课：单变量分布与异常值（45 分钟）

第 1 节课先学“单个变量长什么样”。数据是 `data/05-eda-viz/diamonds.csv`，共 53940 行，主要字段有 `carat`、`cut`、`color`、`clarity`、`price`、`depth`、`table`。

### 1.1 先探索：先看数字，再画图

不要一上来就让 DSH 画图。先运行探索代码，确认字段、类型、分布和样本量：

```python
import pandas as pd

df = pd.read_csv('data/05-eda-viz/diamonds.csv')

print('shape:', df.shape)
print(df.dtypes)
print(df.head())
print(df[['carat', 'depth', 'table', 'price']].describe().round(2))
print()
print('cut:')
print(df['cut'].value_counts())
print('color:')
print(df['color'].value_counts())
print('clarity:')
print(df['clarity'].value_counts())
```

预期：`shape` 是 `(53940, 10)`。看完输出后，能说清楚四件事：

- `describe` 里的 `count`、`mean`、`std`、`min`、`25%`、`50%`、`75%`、`max` 各是什么含义；
- 哪些列是数值列，哪些列是分类列；
- `cut`、`color`、`clarity` 各组样本量是否均衡；
- `price` 的 `75%` 分位与 `max` 差距大不大，这决定直方图会不会出现长尾。

### 1.2 第 1 节课要回答的问题

本节课只回答三个问题：

1. `price` 和 `carat` 的分布是集中还是分散？
2. `cut`、`color`、`clarity` 各类样本量是否均衡？
3. 数据里有没有明显异常值？异常值有多少？

### 1.3 发给 DSH 的提示词

```text
请读取 data/05-eda-viz/diamonds.csv，不要修改原文件。
任务：
1. 输出 shape、describe、cut/color/clarity 的 value_counts；
2. 画一张 1x2 面板：
   - 左图：price 直方图；
   - 右图：按 cut 分组的 price 箱线图；
3. 用 IQR 方法计算 price 的异常值候选数量；
4. 每张子图必须有标题、x/y 轴标签和样本量；
5. 保存到 projects/<姓名>/week5_plots/lesson1_distribution.png。
只使用 pandas 和 matplotlib，不要安装其他包。
```

执行前先检查代码里的关键函数：`read_csv` 读数据，`hist` 画直方图，`boxplot` 画箱线图，`savefig` 写文件。确认 `data/` 没有写操作后再运行。

### 1.4 老师的第一版代码

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv('data/05-eda-viz/diamonds.csv')

print('shape:', df.shape)
print(df[['carat', 'price']].describe().round(2))
print(df['cut'].value_counts())

q1 = df['price'].quantile(0.25)
q3 = df['price'].quantile(0.75)
iqr = q3 - q1
lower = q1 - 1.5 * iqr
upper = q3 + 1.5 * iqr
outliers = df[(df['price'] < lower) | (df['price'] > upper)]
print('IQR lower/upper:', round(lower, 2), round(upper, 2))
print('price outlier candidates:', len(outliers))

cuts = ['Fair', 'Good', 'Very Good', 'Premium', 'Ideal']
prices_by_cut = [df.loc[df['cut'] == c, 'price'].values for c in cuts]

fig, axes = plt.subplots(1, 2, figsize=(12, 4.5))

axes[0].hist(df['price'], bins=50, color='#4C72B0', edgecolor='white')
axes[0].set_title('Diamond price distribution (n = 53940)')
axes[0].set_xlabel('Price (USD)')
axes[0].set_ylabel('Count')

axes[1].boxplot(prices_by_cut, labels=cuts)
axes[1].set_title('Price by cut (n = 53940)')
axes[1].set_xlabel('Cut')
axes[1].set_ylabel('Price (USD)')
axes[1].tick_params(axis='x', rotation=30)

fig.tight_layout()
fig.savefig('projects/<姓名>/week5_plots/lesson1_distribution.png', dpi=150)
plt.show()
```

注意：这张图没有用 `sns`，只用 pandas 和 matplotlib。运行结果要以你自己机器上的输出为准，不要背数字。

### 1.5 解读与审查

用你自己的输出回答三个问题：

- `price` 直方图是右偏还是对称？多数钻石落在哪个区间，长尾在哪里？
- `cut` 各组样本量差多少？样本量最小的组，在比较均值时要特别谨慎。
- IQR 异常值候选有多少？这些是“需要解释的候选值”，不是要直接删除的错误。

箱线图至少能读出：每组的中位数、四分位距、箱外点数量。比较 `cut` 的价格差异时，不能直接说“某种切工导致价格更高”，因为 `carat` 也可能同时更高，这是观察性数据的限制。

图表审查按五条检查：有没有标题、轴标签是否带单位、是否写样本量、是否写数据来源、图注是否说明过滤或刻度。如果高价长尾把箱线图压扁，可以再生成一张 log 刻度版，但必须在图注写明 `y` 轴是 log scale。

### 1.6 第 1 节课验收

- [ ] 能说出 `describe` 中每个统计量的含义
- [ ] 能输出 `cut`、`color`、`clarity` 的 `value_counts`，并指出样本量最少的组
- [ ] 直方图和箱线图都有标题、轴标签、样本量
- [ ] 能计算并报告 `price` 的 IQR 异常值候选数量
- [ ] 没有修改 `data/05-eda-viz/diamonds.csv`，输出在 `projects/<姓名>/week5_plots/`
- [ ] 运行前能解释 `hist`、`boxplot`、`savefig` 分别做什么

## 2. 第 2 节课：分类对比与关系图（45 分钟）

第 2 节课学习两个变量的可视化。数据是 `data/05-eda-viz/midwest.csv`，共 437 行，主要字段有 `county`、`state`、`poptotal`、`popdensity`、`percbelowpoverty`、`percollege`、`perchsd`。

### 2.1 先探索：先审计，再分组

```python
import pandas as pd

df = pd.read_csv('data/05-eda-viz/midwest.csv')

print('shape:', df.shape)
print(df.head())
print(df[['poptotal', 'popdensity', 'percbelowpoverty',
          'percollege', 'perchsd']].describe().round(2))
print(df['state'].value_counts())
```

阅读输出时注意三点：

- 这里是县级数据，不是个人数据；
- `percbelowpoverty`、`percollege`、`perchsd` 都是百分比；
- `state` 各组县数量可能不同，比较州平均值时必须同时报告 `count`。

### 2.2 第 2 节课要回答的问题

1. 各州的平均贫困率差异大吗？每个州各有多少个县？
2. 大学学历比例（`percollege`）与贫困率（`percbelowpoverty`）是否呈负相关？
3. 人口密度高的县，贫困率一定更低吗？

### 2.3 发给 DSH 的提示词

```text
请读取 data/05-eda-viz/midwest.csv，不要修改原文件。
任务：
1. 按 state 分组，输出县数量、平均 percbelowpoverty、平均 percollege；
2. 画一张 1x2 面板：
   - 左图：按平均贫困率排序的条形图；
   - 右图：percollege 对 percbelowpoverty 的散点图，按 state 着色并加图例；
3. 每张子图必须有标题、轴标签和样本量；条形图 y 轴从 0 开始；
4. 保存到 projects/<姓名>/week5_plots/lesson2_compare.png。
只使用 pandas 和 matplotlib。
```

执行前检查：`groupby` 是分组统计，`bar` 画条形图，`scatter` 画散点图，`legend` 加图例。确认代码没有写入原数据目录。

### 2.4 老师的第一版代码

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv('data/05-eda-viz/midwest.csv')

state_stats = (
    df.groupby('state')
    .agg(counties=('county', 'count'),
         avg_poverty=('percbelowpoverty', 'mean'),
         avg_college=('percollege', 'mean'))
    .round(2)
    .sort_values('avg_poverty', ascending=False)
)
print(state_stats)

fig, axes = plt.subplots(1, 2, figsize=(12, 5))

bars = axes[0].bar(state_stats.index, state_stats['avg_poverty'],
                   color='#55A868', label='mean poverty')
axes[0].bar_label(bars, fmt='%.1f')
axes[0].set_title('Average poverty by state (n = 437 counties)')
axes[0].set_xlabel('State')
axes[0].set_ylabel('Percent below poverty')
axes[0].set_ylim(0, state_stats['avg_poverty'].max() * 1.2)
axes[0].legend()

for state, group in df.groupby('state'):
    axes[1].scatter(group['percollege'], group['percbelowpoverty'],
                    s=22, alpha=0.6, label=state)
axes[1].set_title('College rate vs poverty rate (n = 437 counties)')
axes[1].set_xlabel('Percent with college degree')
axes[1].set_ylabel('Percent below poverty')
axes[1].legend(title='State', bbox_to_anchor=(1.02, 1), loc='upper left')

fig.tight_layout()
fig.savefig('projects/<姓名>/week5_plots/lesson2_compare.png', dpi=150)
plt.show()
```

### 2.5 解读与审查

- 分组统计必须同时看 `mean` 和 `counties`：样本量小的州，平均值更容易被单个县拉高或拉低。
- 条形图用于比较分类平均值，`y` 轴必须从 0 开始；如果从 5 开始，3 个百分点的差距会被画得像 15 个百分点。
- 散点图展示的是县层面的关系。如果看到 `percollege` 高、`percbelowpoverty` 低的趋势，只能说“在县级数据上存在负相关”，不能直接说“上大学导致贫困率下降”。
- 散点重叠时用 `alpha` 降低透明度，否则几百个点叠在一起，视觉上会误以为点很少或关系很强。
- 图例、标题、轴标签、样本量缺一不可；这张图的结论只能回答“县级数据里是什么关系”，不能回答个人层面的因果。

### 2.6 第 2 节课验收

- [ ] 分组统计同时包含每个州的县数量和平均值
- [ ] 条形图按平均值排序，`y` 轴从 0 开始，有轴标签和样本量
- [ ] 散点图按 `state` 着色，有图例、`alpha` 透明度和轴标签
- [ ] 能解释为什么比较平均值时要看样本量
- [ ] 能说出一句“这张图不能支持”的结论
- [ ] 输出在 `projects/<姓名>/week5_plots/`，没有修改原数据

## 3. 第 3 节课：时间序列与趋势（45 分钟）

第 3 节课学习带时间的数据。数据是 `data/05-eda-viz/energy_dataset.csv`，共 35064 行、29 列，字段含 `time`、`total load actual`、`price actual` 和各类发电量。字段名较长，且存在缺失值。

### 3.1 先探索：先转时间，先查缺失

```python
import pandas as pd

df = pd.read_csv('data/05-eda-viz/energy_dataset.csv')

print('shape:', df.shape)
print(df.columns.tolist())
print(df.head(3))

missing = df.isna().sum()
print('missing columns:')
print(missing[missing > 0].sort_values(ascending=False))

df['time'] = pd.to_datetime(df['time'], utc=True)
df = df.sort_values('time')
print('time range:', df['time'].min(), '->', df['time'].max())
```

关键规则：**字段名长，不要手敲，用 `df.columns.tolist()` 复制**；`time` 必须转成 `datetime` 才能重采样。缺失值先报告数量和位置，再决定画图时怎么处理。

### 3.2 第 3 节课要回答的问题

1. 总负荷随时间是否有日、周或年周期？
2. 电价（`price actual`）与总负荷（`total load actual`）是否同向变化？
3. 缺失值出现在哪些列？画图时如何报告，而不是悄悄填充？

### 3.3 发给 DSH 的提示词

```text
请读取 data/05-eda-viz/energy_dataset.csv，不要修改原文件。
任务：
1. 把 time 转成 datetime，按 time 排序；
2. 输出缺失值列和数量；
3. 按天重采样 total load actual 和 price actual 的均值；
4. 画两张上下排列的折线图：日平均负荷、日平均电价；
5. 每张图必须有标题、轴标签、图例和样本量；图注说明重采样方式与缺失处理；
6. 保存到 projects/<姓名>/week5_plots/lesson3_trend.png。
只使用 pandas 和 matplotlib。
```

执行前检查：`to_datetime` 转换时间，`resample` 聚合，`plot` 画折线，`savefig` 写文件。缺失处理必须是“报告后再决定”，不是直接 `fillna(0)`。

### 3.4 老师的第一版代码

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv('data/05-eda-viz/energy_dataset.csv')

df['time'] = pd.to_datetime(df['time'], utc=True)
df = df.sort_values('time')

missing = df.isna().sum()
print('missing columns:')
print(missing[missing > 0].sort_values(ascending=False))

daily = (
    df.set_index('time')[['total load actual', 'price actual']]
    .resample('D')
    .mean()
)
print('daily rows:', len(daily))
print('daily rows with any missing:', int(daily.isna().any(axis=1).sum()))
print(daily.head())

fig, axes = plt.subplots(2, 1, figsize=(11, 7), sharex=True)

axes[0].plot(daily.index, daily['total load actual'],
             color='#4C72B0', linewidth=1, label='daily mean')
axes[0].set_title('Daily mean total load (35064 hourly rows; NaN days left as gaps)')
axes[0].set_ylabel('Total load actual (MW)')
axes[0].legend()

axes[1].plot(daily.index, daily['price actual'],
             color='#C44E52', linewidth=1, label='daily mean')
axes[1].set_title('Daily mean electricity price (35064 hourly rows; NaN days left as gaps)')
axes[1].set_xlabel('Date')
axes[1].set_ylabel('Price actual (EUR/MWh)')
axes[1].legend()

fig.tight_layout()
fig.savefig('projects/<姓名>/week5_plots/lesson3_trend.png', dpi=150)
plt.show()
```

`resample('D').mean()` 会按天平均，缺失的小时不会被计入该天均值；若某天全部缺失，折线图上会留下缺口。这个缺口是真实信息，不要为了让图好看而随便补数。

### 3.5 解读与审查

- 不转 `datetime` 就直接 `resample` 会报错或得到错误分组，先 `to_datetime` 再 `sort_values`。
- 日平均值会平滑小时级波动。要看日内峰谷，需要小时级折线；要看长期趋势，用日或周平均更清楚。
- 缺失值处理不是“补成 0”，而是先报告数量，再决定保留缺口、`dropna` 或明确说明后插值。大段缺失时插值会制造假趋势。
- 长字段名如 `total load actual`、`price actual` 要原样复制，不要缩写、不要拼写猜测。
- 折线图必须有标题、轴标签、单位、图例或必要的图注，并且写明“按天均值、缺失保留为缺口”。

### 3.6 第 3 节课验收

- [ ] `time` 已用 `to_datetime` 转换并按时间排序
- [ ] 输出了缺失值列和数量
- [ ] 能用 `resample('D')` 得到日平均负荷与日平均电价
- [ ] 两张折线图都有标题、轴标签、图例和样本量
- [ ] 能说出图中能否看到周期，以及观察到的周期类型
- [ ] 没有用 `fillna(0)` 掩盖缺失，图注写明了缺失处理方式
- [ ] 没有修改 `data/05-eda-viz/energy_dataset.csv`

## 4. 本周验证清单

- [ ] 每张图都对应一个可指认的具体问题
- [ ] 每节课都先做数据审计，再提问题，最后画图
- [ ] 所有图都有标题、轴标签、图例或必要说明、样本量
- [ ] 读数据路径都是 `data/05-eda-viz/...`，输出都在 `projects/<姓名>/week5_plots/`
- [ ] 使用 DSH 前先看计划，执行前能解释命令，运行后验证输出
- [ ] DSH 生成的绘图代码已经逐行看懂，没有未理解的 `savefig`、删除或写入操作
- [ ] 第 1、2、3 节课验收清单全部通过
- [ ] 能说出每张图不能回答什么问题

## 5. 常见错误与坑

| 现象 | 原因 | 处理 |
|---|---|---|
| 图好看但不知道在比什么 | 没有先定义问题 | 先写“这张图回答什么问题”，再画图 |
| 图没有标题、轴标签或单位 | 只追求“出图” | 按“标题、x 轴、y 轴、单位、n”逐项检查 |
| 条形图差距被夸大 | `y` 轴不从 0 开始 | 条形图 `y` 轴从 0 开始，不截断 |
| 分组统计没有样本量 | 只看了 `mean` | 每个分组同时输出 `count` |
| 几百个点叠在一起看不清 | 散点没有透明度和大小控制 | 加 `alpha=0.4~0.7`，必要时抽样并写明 |
| 对 `time` 直接 `resample` | 时间还是字符串 | 先 `pd.to_datetime`，再排序 |
| 长字段名拼错 | 手敲英文列名 | 用 `df.columns.tolist()` 复制 |
| 缺失值直接填 0 | 为了“让图完整” | 先报告数量；缺失要保留或明确说明处理方式 |
| 缺失值太多还强行插值 | 把缺口画成了真实趋势 | 大段缺失时保留缺口，并在图注写明 |
| 异常值直接删除 | 把“候选异常”当成“错误” | 先报告数量和区间，再决定是否过滤或分开展示 |
| 图表没写过滤或刻度 | 读图的人不知道口径 | 图注写清过滤规则、log 刻度、重采样方式 |
| 看到相关就说因果 | 混淆相关与因果 | 结论里写“观察到相关”，并说明数据层级 |
| DSH 生成的代码直接运行 | 没审查就执行 | 先逐行解释；看不懂就让 DSH 解释后再运行 |
| 把图保存到 `data/` | 忘记目录边界 | 输出固定写到 `projects/<姓名>/week5_plots/` |

## 6. 作业

1. 把三节课代码整理成一个可运行脚本 `projects/<姓名>/week5_eda_viz.py`，生成 3 张图到 `projects/<姓名>/week5_plots/`，路径和数据与本周正文一致。
2. 写 `projects/<姓名>/week5-conclusions.md`，给出 3 条结论。每条结论必须写：使用的图表、数据文件、字段、样本量、计算或画图方法，以及这条结论不能支持什么。
3. 让 DSH 审查三张图的绘图代码，专门检查“标题、轴标签、图例、样本量、缺失处理、过滤说明、保存路径”，记录第一版代码和修改后的代码，并说明每处修改为什么必要。

## 评分要点

| 项目 | 要求 |
|---|---|
| 数据审计 | 能输出 `shape`、`describe`、`value_counts`、缺失值并解释含义 |
| 图形选择 | 单变量、分类对比、关系、时间序列分别使用匹配的图型 |
| 图表完整 | 标题、轴标签、单位、图例或必要说明、样本量齐全 |
| 不误导 | `y` 轴、透明度、过滤、缺失和异常值处理都在图注中说明 |
| DSH 安全 | 先审计划、检查命令、小步执行、验证结果，代码先看懂再运行 |
| 结论 | 每条结论都能指出数据、字段、计算方法和局限性 |
